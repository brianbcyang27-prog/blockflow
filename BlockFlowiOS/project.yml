name: BlockFlowiOS
options:
bundleIdPrefix: com.blockflow
deploymentTarget:
iOS: "15.0"
xcodeVersion: "15.0"
generateEmptyDirectories: true

settings:
base:
SWIFT_VERSION: "5.9"
TARGETED_DEVICE_FAMILY: "1,2"

targets:
BlockFlowiOS:
type: application
platform: iOS
sources:
- path: Sources
- path: Resources
- path: Info.plist
settings:
base:
PRODUCT_BUNDLE_IDENTIFIER: com.blockflow.app
INFOPLIST_FILE: Info.plist
GENERATE_INFOPLIST_FILE: NO
MARKETING_VERSION: "1.0.0"
CURRENT_PROJECT_VERSION: "1"
ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
DEVELOPMENT_TEAM: 2MBBTV3LWH
CODE_SIGN_STYLE: Automatic
CODE_SIGN_IDENTITY: "-"

schemes:
BlockFlowiOS:
build:
targets:
BlockFlowiOS: all
run:
config: Debug
test:
config: Debug
profile:
config: Release
analyze:
config: Debug
archive:
config: Release